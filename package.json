{
  "name": "math",
  "type": "module",
  "author": "Damjan Košir",
  "license": "AGPL-3.0-only",
  "scripts": {
    "build": "bun install && bun scripts/update-reward-count.ts && mkdir -p dist/rpg && cp src/*.html dist && cp src/favicon-32x32.png dist && cp -r src/rewardImages dist && cp src/rpg/*.html dist/rpg && cp -r src/rpg/assets dist/rpg && bun build ./src/app.ts ./src/index.ts ./src/practice.ts ./src/rpg/rpg.ts ./src/rpg/actors.ts --outdir dist",
    "dev": "bun run build --watch",
    "caddy": "mkdir -p dist/rpg && cd dist && caddy file-server --listen :3000",
    "debug": "chromium --remote-debugging-port=9222 --user-data-dir=remote-debug-profile http://localhost:3000/rpg/rpg.html",
    "sim": "bun run ./src/rpg/simulator.ts"
  },
  "devDependencies": {
    "prettier": "^3.6.2",
    "typescript": "^5.9.3"
  },
  "dependencies": {
    "pixi.js": "^8.15.0"
  }
}