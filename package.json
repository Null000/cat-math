{
  "name": "math",
  "packageManager": "yarn@4.10.3",
  "type": "module",
  "author": "Damjan Košir",
  "license": "AGPL-3.0-only",
  "scripts": {
    "build": "mkdir -p dist && cp src/*.html dist && cp -r src/rewardImages dist && bun build ./src/app.ts ./src/index.ts ./src/practice.ts --outdir dist",
    "dev": "mkdir -p dist && cp src/*.html dist && cp -r src/rewardImages dist && bun build ./src/app.ts ./src/index.ts ./src/practice.ts --outdir dist --watch",
    "caddy": "cd dist && caddy file-server --listen :3000"
  },
  "devDependencies": {
    "prettier": "^3.6.2",
    "typescript": "^5.9.3"
  }
}